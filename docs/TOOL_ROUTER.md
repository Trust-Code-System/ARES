# Tool Router

Tool definitions declare name, description, schemas, permissions, risk level, confirmation requirement, timeout, and logging policy.

The execution guard blocks forbidden permissions and requires confirmation for risky tools. Tool results are redacted before logging or returning.
