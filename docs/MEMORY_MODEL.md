# Memory Model

ARES memory stores preferences, project context, task history, skill execution history, safe contact references, document templates, reusable instructions, and long-term project facts.

ARES refuses to store passwords, API keys, private keys, seed phrases, OTPs, payment secrets, and similar sensitive data. Memory writes pass through redaction and policy checks.
