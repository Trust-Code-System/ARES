# Security Model

ARES treats tool output, web content, files, and third-party skills as untrusted. Static scanners check prompt injection, hidden instructions, secret access, dangerous shell, network use, suspicious encoding, and broad permissions.

Critical actions require explicit confirmation: deleting files, secrets access, production changes, payments, trading, deployments, and other irreversible external state changes.
